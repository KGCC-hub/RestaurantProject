apiVersion: v1
kind: Service
metadata:
  name: {{ include "restaurant-chart.fullname" . }}
  labels:
    {{- include "restaurant-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "restaurant-chart.selectorLabels" . | nindent 4 }}
